apiVersion: v1
kind: Secret
metadata:
  name: quakewatch-secret
  namespace: quakewatch
type: Opaque
stringData:
  # stringData lets you put plain text (kubectl will encode it), safer for examples
  SECRET_KEY: "super-secret-value"
  DB_PASSWORD: "example-db-password"
